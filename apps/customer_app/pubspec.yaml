name: customer_app
description: HANDS customer app for Vietnam on-demand partner booking.
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  app_links: 7.0.0
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  flutter_riverpod: ^2.6.1
  go_router: ^14.8.1
  http: ^1.2.2
  image_picker: ^1.1.2
  maplibre_gl: ^0.26.0
  geolocator: ^13.0.2
  flutter_secure_storage: ^9.2.4
  socket_io_client: ^3.0.2
  supabase_flutter: ^2.12.4
  url_launcher: ^6.3.2
  firebase_core: ^4.10.0
  firebase_messaging: ^16.3.0
  flutter_local_notifications: ^22.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
