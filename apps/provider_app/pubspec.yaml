name: provider_app
description: HANDS partner app for Vietnam on-demand service operations.
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.6.1
  go_router: ^14.8.1
  http: ^1.2.2
  maplibre_gl: ^0.26.0
  geolocator: ^13.0.2
  flutter_secure_storage: ^9.2.4
  socket_io_client: ^3.0.2
  supabase_flutter: ^2.12.4
  image_picker: ^1.2.2
  firebase_core: ^4.10.0
  firebase_messaging: ^16.3.0
  flutter_local_notifications: ^22.0.0
  geocoding: ^4.0.0
  url_launcher: ^6.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
